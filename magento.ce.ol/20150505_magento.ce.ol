(work) две ветки - master, OrderGridIssueBad.
Обе ветки содержат ошибку OrderGrid

(home) две ветки - master, OrderGridIssue.
Обе ветки не содержать ошибку OrderGrid.

Как с машины (work) залить на ветку OrderGridIssueBad на сервер?
